---
apiVersion: v1
kind: Service
metadata:
  name: traefik-dashboard
  namespace: traefik

spec:
  selector:
    app: traefik
  ports:
  - name: web
    protocol: TCP
    port: 8081
    targetPort: 80
